/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import LanguageApp from './src/LanguageApp';

AppRegistry.registerComponent(appName, () => LanguageApp);
